<!-- Intestazione / Brand -->
<div class="h-16 flex items-center px-6 bg-slate-950 border-b border-slate-800/60 shadow-sm">
    <a class="text-xl font-black tracking-wider text-white no-underline flex items-center gap-2" href="">
        <span class="text-blue-500 font-bold">⚡</span> BidWinAI
    </a>
</div>

<!-- Input per il Menu Mobile -->
<input type="checkbox" title="Navigation menu" class="navbar-toggler hidden" id="nav-toggle" />

<!-- Contenitore Navigazione Laterale -->
<div class="nav-scrollable flex flex-col bg-slate-900 text-slate-300 min-h-[calc(100vh-4rem)] pt-4">
    <nav class="flex flex-col gap-1.5 px-3">

        <!-- Link: Home -->
        <div class="w-full">
            <NavLink
                class="flex items-center gap-3.5 px-4 py-3 text-slate-400 hover:text-white hover:bg-slate-800/50 rounded-xl transition-all duration-200 font-medium no-underline text-sm group [&.active]:bg-slate-800/70 [&.active]:text-white [&.active]:font-semibold"
                href="" Match="NavLinkMatch.All">
                <span class="text-lg group-hover:scale-110 transition duration-150">🏠</span>
                <span>Home</span>
            </NavLink>
        </div>

        <!-- Link: Carica Bando -->
        <div class="w-full">
            <NavLink
                class="flex items-center gap-3.5 px-4 py-3 text-slate-400 hover:text-white hover:bg-slate-800/50 rounded-xl transition-all duration-200 font-medium no-underline text-sm group [&.active]:bg-slate-800/70 [&.active]:text-white [&.active]:font-semibold"
                href="carica-bando">
                <span class="text-lg group-hover:scale-110 transition duration-150">📤</span>
                <span>Carica Bando</span>
            </NavLink>
        </div>

        <!-- Link: Archivio Bandi -->
        <div class="w-full">
            <NavLink
                class="flex items-center gap-3.5 px-4 py-3 text-slate-400 hover:text-white hover:bg-slate-800/50 rounded-xl transition-all duration-200 font-medium no-underline text-sm group [&.active]:bg-slate-800/70 [&.active]:text-white [&.active]:font-semibold"
                href="archivio">
                <span class="text-lg group-hover:scale-110 transition duration-150">📌</span>
                <span>Archivio Bandi</span>
            </NavLink>
        </div>

    </nav>
</div>